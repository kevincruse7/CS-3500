We managed to get all of the features of the provided view working, including starting/stopping,
restarting, looping, and speeding up/slowing down. The view is rather slow, however, since it uses
some O(n^2) algorithms to render shapes due to some design limitations of their model interfaces.